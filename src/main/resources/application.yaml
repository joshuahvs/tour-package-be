spring:
  application:
    name: tour-package-be

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  profiles:
    active: "dev"

  jpa:
    hibernate:
      ddl-auto: create-drop

security:
  jwt:
    jwtSecret: "${JWT_SECRET_KEY}"
    jwtExpirationMs: 86400000
