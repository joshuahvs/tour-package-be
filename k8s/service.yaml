apiVersion: v1
kind: Service
metadata:
  name: tour-package-be-service
  namespace: default
spec:
  selector:
    app: tour-package-be
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080